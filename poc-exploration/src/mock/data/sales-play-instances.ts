// Per-account sales-play instance records.
// Populated by Anna's CSV ingest — empty until then.

import type { SalesPlayInstance } from '../types';

export const SALES_PLAY_INSTANCES: SalesPlayInstance[] = [];
